name: GeoSpec Dashboard Status

# NOTE: Monitoring runs LOCALLY via run_and_publish.ps1
# Source code is not in this repo (IP protection)
# This workflow is informational only

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Dashboard Info
        run: |
          echo "=================================================="
          echo "  GeoSpec Ensemble Monitoring"
          echo "=================================================="
          echo ""
          echo "  Dashboard: https://kantrarian.github.io/geospec/"
          echo ""
          echo "  Monitoring runs locally (source code protected)"
          echo "  Use: run_and_publish.ps1"
          echo "=================================================="
