# fault2graph Phase B — Power Annex **B2A** (temporal community coherence, amended family) v1

**Author:** cayley — **Date:** 2026-08-20 — **Status:** DRAFT until hash-bound in the seal manifest.
**Family under test:** frozen AMENDMENT 1 §A2 exactly (freeze `F2G-PB-A1-R3`, authority `7c3ca7b` / `f3d0830b…`): statistic `R_total = Σ_bound-carriers (maximal runs of identical partitions over calendar-adjacent eligible days)`, one-sided LOW; null = ONE common permutation of the entire fixed 50-position joint evaluation-day capsule per draw (partitions, typed refusals, frames, gap metadata atomic; recompute after), complete group incl. identity; gaps and `NODESET_MISMATCH` terminate runs, never bridged; carrier binding + family-level withholding per frozen §A2; `p = (1 + #{R_null ≤ R_obs})/(N_valid + 1)`, 9,999 draws, floor.
**Inherits:** COMMON PROTOCOL rev-1.2 @ `2d891ff`, sha256 `00fd25ba1e477fcaebe1801bcffa4db66c3c63957d0de16a478ed9da2a459314` (normative).

## Registered effect (unchanged effect class from superseded annex B-2)

Two-block latent structure on carrier 1 (within-block +0.9 / cross −0.5 pre-tanh, constructed on the accepted side of all four identifiability gates on clean days); at evaluation position 25, `m` stations per block exchange membership persistently (lexicographically last `m` of block A ↔ first `m` of block B). Carriers 2–3 carry the common generator (γ=0.05 shared factor) without block structure.

## Registered grid (3 points)

`m ∈ {1, 2, 3}`, ties ascending. Registered maximum meaningful effect: `m = 3` persistent from mid-window.

## Inversion note on §L (disclosed)

The superseded annex B-2's §L lemma (day-order permutation degeneracy, p ≡ 1) applies to the OLD max-switch statistic and stands as evidence for the supersession. For B2A the SAME two-regime fixture must move to the OPPOSITE extreme: few observed runs vs permuted-shattered runs ⇒ p at the add-one floor — this is the frozen §A5 mandatory red-KAT expectation, and these tables measure how far it survives noise, MCAR-induced refusals, and the noise carriers' run contributions.

## Recovery, certification, terminals

Per common §3 verbatim (post-LOCO endpoint, top-8 pre-screen selector, binomial-LB certification with stopping rule, Pareto frontier, `MDE_NOT_CERTIFIED_BY_REGISTERED_SEARCH` terminal, Tier-S = `PRELIMINARY_SMOKE` only). `B2A` substreams under the frozen-amendment seed root.

*Power annex B2A v1 — cayley. Fixture-only; no real graphs.*
