# fault2graph Phase B — Power-Annex COMMON PROTOCOL v1 **rev-1.5** (shared by ALL family annexes, incl. amended B1A/B2A/B3A)

**Author:** cayley — **Date:** 2026-08-20 — **Status:** DRAFT until the family annexes are hash-bound in the seal manifest. This file is referenced BY DIGEST from each family annex; it is not itself a manifest slot. **rev-1.1** incorporated codex 0348Z repairs 1–4 (three-segment frozen geometry; post-LOCO Tier-C selector; `MDE_NOT_CERTIFIED_BY_REGISTERED_SEARCH` terminal type; Pareto-frontier reporting). **rev-1.2** extends the machinery to the amended families under freeze `F2G-PB-A1-R3-FREEZE-CODEX-20260820T1325Z` (authority `7c3ca7b`, LF sha `f3d0830b…`): (a) the generator gains the registered SHARED CALENDAR-DAY FACTOR — `u_ab(d) = mu0 + γ·G(d) + s_a(d) + s_b(d) + eps_ab(d)` with `G(d) ~ N(0,1)` drawn ONCE per calendar day and common to ALL carriers, `γ = 0.05` for MDE estimation; a separate `γ = 1.0` SYNCHRONY FIXTURE is registered for the paired-carrier red-KATs only (never for MDE estimation) — instantiating the frozen joint carrier law; (b) amended-family substreams use tokens `B1A`/`B2A`/`B3A` with seed root = the FROZEN AMENDMENT's LF sha256 (`f3d0830b…`, per frozen §A4), fold/purpose grammar unchanged; (c) Tier-C exact pipeline for amended families = the amended engine functions under the frozen §A0b joint transforms (`S_11` equal-block / atomic day-capsule / shared-registry relabeling) at 9,999 draws with the frozen carrier binding — a replicate in which a bound carrier is unscorable is typed per frozen `CANNOT_DETERMINE_FAMILY_SCORABILITY` semantics and counts as a disclosed non-recovery. All tiers, certification/stopping rules, Pareto-frontier reporting, terminal types, and the equivalence-gate requirement apply to amended families verbatim. Digest history: v1 `91125cc2…` → rev-1.1 `baddf2aa…` → rev-1.2 `00fd25ba…` (header-only G-factor, superseded by rev-1.3 which makes §1/§4 normative per codex 1411Z fixes 1-2) (both remain addressable at their pinned commits for the superseded families' evidence).

**Frozen-contract bindings:** prereg rev-2 `a44819d` / sha256 `d1929c3127f6d76b87e95319e0f56c4d8ddf4b0cd6226aa9c2e1fe564e44e04e`; freeze `F2G-PB-R2-FREEZE-CODEX-20260820T0301Z`; engine `monitoring/src/d2_f2g_phase_b_stats.py` @ `6034419` (byte-stable); codex rulings `633be612` (0319Z items 3–5) and `2574cdd8` (0330Z items 1–5), both incorporated normatively below. FIXTURE-ONLY: nothing here touches the Phase-A artifact, any real graph, or any waveform.

## 1. Registered synthetic generator (domain-respecting, codex 0319Z item 5)

**Latent station-factor Fisher-z model.** For carrier c with station set S_c, edge (a,b), day d:

- **Amended-family lane (B1A/B2A/B3A, the normative formula for all new estimation):** `u_ab(d) = mu0 + γ·G(d) + s_a(d) + s_b(d) + eps_ab(d)`, with `G(d) ~ N(0, 1)` drawn ONCE per calendar day and SHARED BY ALL CARRIERS (the frozen joint carrier law), `γ = 0.05` for all MDE estimation; the paired-carrier synchrony red-KAT fixture ALONE uses `γ = 1.0` and is never used for MDE. `s_x(d) ~ N(0, sigma_s^2)` iid per (station, day), `eps_ab(d) ~ N(0, sigma_e^2)` iid per (edge, day).
- **Superseded-family lane (B1/B2/B3, historical):** the pinned generator of rev-1.1 (`u_ab(d) = mu0 + s_a(d) + s_b(d) + eps_ab(d)`, no `G` term) remains the evidence-generator of the completed `98055ca` search and is never used again.
- **Registered PCG64 draw order per replicate (amended lane; insertion of `G` cannot re-key later surfaces because the entire lane uses fresh `B1A/B2A/B3A` substreams at the frozen-amendment seed root):** (1) `G[110]` first, one vector for the shared calendar; then (2) for each carrier in sorted order: station shocks `s` (n_st × 110, row-major), edge noise `eps` (n_edges × 110, row-major, edges sorted), MCAR mask (n_edges × 110); then (3) family effect placement draws (onset positions where the family grid says seeded).
- **`r_ab(d) = tanh(u_ab(d))`** — guaranteed finite in (−1, 1) with NO post-hoc clipping.
- Registered parameters: `mu0 = atanh(0.30)` (baseline coherence 0.30), `sigma_s = 0.15`, `sigma_e = 0.20`.
- Dependence construction (disclosed): WITHIN a carrier, cross-edge dependence enters through the shared station shocks `s_x(d)`; ACROSS carriers (amended lane), same-day dependence enters through the shared calendar factor `γ·G(d)` — the joint carrier law the frozen amendment's common transforms preserve. Whole carrier-day vectors are generated jointly; no independent edge noise is presented as dependence-realistic.
- **Panel geometry (real-carrier scale):** 3 carriers with 12/12/11 stations (35 total, matching the frozen selected registry count), 110 registered days each (positions 1–60 baseline / 61–110 evaluation). **Segments (codex 0348Z item 1): THREE synthetic segment labels per carrier with the EXACT frozen registry size vectors** — carrier 1: 4/4/4, carrier 2: 4/4/4, carrier 3: 4/4/3 — assigned by sorted station index (first 4 → seg_1, next 4 → seg_2, remainder → seg_3). B-2's planted two-community partition is an EFFECT OBJECT and is distinct from this segment registry. **Missingness:** 8% MCAR at the (edge, day) cell level (matching the Phase-A order of magnitude, 272/3849 ≈ 7%), applied before any effect injection.
- Effects are injected in LATENT units (`delta_lat` added to `u` before tanh) on family-specific supports defined in each family annex. Each results table reports the empirically induced robust-z distribution of the injected cells so latent units are interpretable on the frozen z scale.

## 2. Registered randomness (codex 0330Z item 5)

- **Replicate stream:** `master_r = PCG64(derive_substream_seed(frozen_doc_sha256, <family>, "full", "power"))`. Replicate index r (0-based) uses `rep_seed = the r-th sequential uint64 draw` from `master_r`; the replicate's panel (noise, missingness mask, injection placement where the family grid says "seeded") is generated entirely from `Generator(PCG64(rep_seed))`.
- **Null draws inside the engine** use the frozen null substreams (`purpose=null`, fold-specific) exactly as in production; consequently the null offset/permutation sequences are COMMON across replicates for a fixed frozen doc. Disclosed and permitted per codex 0330Z item 5.

## 3. Estimation tiers (codex 0330Z items 1–4)

- **Tier-S `PRELIMINARY_SMOKE`:** R = 50 replicates per grid point, engine called with `n_draws = 999`. Purpose: full-grid landscape + Tier-C candidate selection ONLY. Tier-S tables are labeled `PRELIMINARY_SMOKE` in every artifact and can NEVER populate a certified MDE or a passed power contract.
- **Tier-C `CERTIFICATION` (exact production pipeline):** `n_draws = 9,999`, frozen valid-draw floor, add-one p, and the final **post-LOCO family-positive rule**: a replicate counts as a recovery only if the full-data family passes α = 0.05/3 AND `loco_gate` passes over ALL 35 station-drop folds (each fold its own frozen `loco:<STATION_ID>` null substream). Pre-LOCO and post-LOCO recovery are reported separately; **all certified-frontier claims follow post-LOCO recovery.**
- **Tier-C candidate selection (registered, deterministic; codex 0348Z item 3 — the selector targets the post-LOCO endpoint):** stage 1: rank all grid points by Tier-S pre-LOCO recovery and keep the top 8 (coordinate tie-break); stage 2: for those 8 points, compute Tier-S **post-LOCO** recovery (R = 50, all 35 folds, `n_draws = 999`); the 3 Tier-C candidates are the highest Tier-S post-LOCO recoveries (coordinate tie-break). Pre-LOCO recovery is reported as a diagnostic only. The stage-1 pre-screen is a registered compute bound and is disclosed as such — points outside the top 8 are smoke-screened only.
- **Certification rule:** a point certifies iff the one-sided exact-binomial 95% LOWER confidence bound on post-LOCO recovery is ≥ 0.80.
- **Replicate cap + deterministic stopping rule:** evaluate at R = 20; if the 95% lower bound ≥ 0.80 → CERTIFIED; else if the one-sided 95% exact-binomial UPPER bound < 0.80 → FAILED; else extend to R_max = 40 and re-apply the lower-bound rule; if the interval still straddles 0.80 at R_max → typed `CANNOT_DETERMINE_POWER_ESTIMATE` (never rounded into certification).
- **Detectable-effect frontier (codex 0348Z item 4):** the grids are multidimensional, so there is no unique scalar MDE. The results report EVERY certified point and the coordinatewise Pareto-minimal certified frontier; the deterministic representative for G5/G16 is the lexicographically first Pareto-minimal certified point. No single point is called "the" scientific MDE.
- **No-power terminal type (codex 0348Z item 2):** if no Tier-C candidate certifies, the family registers **`MDE_NOT_CERTIFIED_BY_REGISTERED_SEARCH`** — an explicit statement that the three selected candidates failed the exact pipeline and all OTHER grid cells were smoke-screened, NOT exact-pipeline-bounded. A stronger `MDE_NOT_ATTAINED` claim requires either Tier-C on every grid point or a mathematical support proof encoded and executably checked (B-2 annex §L). Either terminal state satisfies frozen §5: absent a certified point, every nonpositive is typed `CANNOT_DETERMINE_NO_POWER`.

## 4. Computational equivalence (memoized drivers — split by lane, codex 1411Z fix 2)

**Superseded lane (B1/B2/B3, historical evidence only):** the engine `6034419` + driver `d4edfb2` finite-rotation receipt (≤ 110 distinct offsets, byte-equal full + fold) is the evidence of the completed `98055ca` search and establishes NOTHING for any amended family.

**Amended lane (B1A/B2A/B3A):** each family binds the AMENDED engine commit and any estimation-driver commit at results time, and the driver must prove EXACT full-data AND every-LOCO-fold p/verdict equality against the amended engine on registered fixtures under the new transforms before any table is admissible. No ≤ 110-lookup claim is inherited. **Registered exact reduction for B1A (to be proven, not assumed):** under the frozen 11×10-day blocks the 60/50 split is BLOCK-ALIGNED (baseline = 6 block-slots, evaluation = 5 block-slots), and the per-edge baseline median/MAD depend only on WHICH 6 blocks occupy the baseline (median is order-invariant) — `C(11,6) = 462` distinct baseline block-sets per replicate are memoizable, with window means then computed on each draw's permuted evaluation order. The byte-equal gate (full + every fold) against the amended engine is mandatory before use; if it fails, the driver runs the engine directly. B2A/B3A drivers may call the amended engine directly (their per-draw costs are small); any B2A/B3A memoization requires its own specified reduction + gate.

## 5. Results artifacts

Each family annex is accompanied by `<annex>_results.json`: tier-labeled tables (every registered grid point for Tier-S; every candidate for Tier-C with pre/post-LOCO recovery, exact-binomial bounds, stopping-rule trace), the induced robust-z report, the equivalence receipt, engine/doc/annex digests, and env lock. Results artifacts are digest-routed with the codex receipt; they are evidence, not registered content — the registered content is the annex bytes.

*Common protocol v1 — cayley. Referenced by digest from annexes B-1/B-2/B-3.*

## 6. CALENDAR-FRAME (true-geometry) estimation lane — rev-1.4, under freeze F2G-PB-A2-R2-FREEZE-CODEX-20260821T005735Z

This section supersedes sections 1-4's GEOMETRY for all NEW estimation (the amended-lane 110-day equal-panel geometry of rev-1.2/1.3 is CONSUMED historical evidence; its B2A certification carries zero weight per the frozen Amendment 2). Everything not restated here (tiers, R and draw counts, post-LOCO endpoint, top-8 pre-screen selector, Clopper-Pearson certification and stopping rules, Pareto-frontier reporting, `MDE_NOT_CERTIFIED_BY_REGISTERED_SEARCH` terminal, no-power routes, blob-authority verification discipline) applies verbatim.

- **Panel geometry = THE RUN OBJECT'S GEOMETRY:** the 132-position shared calendar and the exact per-carrier registered-day masks of the bound authority (`docs/f2g_phase_b_shared_calendar_v1.json` @ `8111805`, blob sha `3aaaec5872933adf3e7f0daa7beb840e11b793a41d7c387ad4800c078fc65111`; masks 111/108/111, dual-witnessed). **Power panels are emitted with `calendar_authority_mode: "bound"`** (codex 0131Z repair): the generator synthesizes VALUES only, on the real frozen carrier names, the real calendar, and the exact real masks, so the engine's bound-mode byte-verification applies to every power panel; values exist ONLY on mask days; `NO_REGISTERED_SNAPSHOT` positions are genuinely absent, never synthesized. `"fixture"` mode is for UNIT FIXTURES ONLY and is never admissible as a power-estimation input. Every power-result artifact must echo `calendar_authority_mode`, `calendar_authority_sha256 = 3aaaec58…`, and the input-panel digest; the power-result verifier refuses missing/non-bound mode, a wrong authority digest, or an unreopenable panel digest, typed `POWER_GEOMETRY_UNBOUND` (or the existing authority/mask mismatch codes).
- **Generator (registered draw order, calendar lane):** replicate rng from the `purpose=power` substream at the FROZEN AMENDMENT-2 seed root (`58b513b6c30b70c8014510788da9d7d819ce8971ca59b7dfdc11c57a1664586f`); draws: (1) `G[132]` first — one shared factor value per CALENDAR day, `γ = 0.05` (synchrony fixture `γ = 1.0` red-KAT-only); (2) per carrier in sorted order: station shocks `s` (n_st × n_mask_days, row-major over the carrier's MASK days in calendar order), edge noise `eps` (n_edges × n_mask_days), MCAR mask (n_edges × n_mask_days, 8%); (3) family effect placement. `u_ab(d) = mu0 + γ·G(cal_index(d)) + s_a(d) + s_b(d) + eps_ab(d)` on mask days only; `r = tanh(u)`; parameters `mu0/sigma_s/sigma_e` unchanged. The three-segment frozen registry sizes (4/4/4, 4/4/4, 4/4/3) and 12/12/11-station carriers stand.
- **Family effects (calendar lane):** injections address EVALUATION-WINDOW MASK DAYS (calendar positions 73-132 intersected with the carrier mask). B1A: `delta_lat` on the `n_e` lex-first edges of carrier 1 over `k` consecutive MASK days of the eval window (start seeded among valid starts); B2A: the two-community effect object with the membership swap at the carrier's first eval-window mask day at or after calendar position 97 (mid-window), persistent; B3A: `delta_lat` on `n_cross` lex-first cross-segment edges over `k` consecutive eval-window mask days. Grids and outer bounds unchanged from the family annexes.
- **Pipelines under estimation = the calendar engine seams** `b1a_family_cal` / `b2a_family_cal` / `b3a_family_cal` at the Amendment-3-admitted engine, with the frozen calendar-position window rule, S_11/S_60 transforms, and typed-absence semantics. LOCO folds = the 35 real-registry-sized station drops, fold substreams at the Amendment-2 root.
- **Equivalence gates (calendar lane):** any estimation-driver reduction must prove exact full-data AND every-fold p/verdict equality against the admitted calendar engine on registered fixtures before any table row; on failure, direct engine calls. Evidence capsules bind COMMITTED GIT BLOBS only.
- **Digest history addendum:** rev-1.3 `b6352e91…` remains the authority for the consumed equal-panel campaign's evidence.
