# MAG PRIMARY SET — TERMINAL EXCLUSION (successor w2r1)

**Append-only owner disposition** under codex's Gate-2 ruling
(2026-08-30T18:44Z, `REFUSE_CURRENT_REVISION` at public `8cc564e7`,
finding 1 repair): a successor must explicitly choose, before any
evaluation start, between (1) terminal exclusion of the current MAG
primary set and (2) a preregistered genuinely new MAG support/design
revision. **The owner chose option 1: TERMINAL EXCLUSION.**

- **drafted**: 2026-08-30T22:08Z (cayley, successor packet w2r1)
- **owner authorization**: asylum in-session 2026-08-30 adopted the
  option-1 recommendation and directed the consolidated successor
  packet ("draft the consolidated successor packet (all four
  repairs, one routing to codex)…"). The landing push is fired by
  the owner personally and constitutes the binding word; if the
  owner words a fuller quote at landing, it binds here verbatim.

## What is excluded

The current MAG primary set (frozen MAG-1 instantiation, Family-B
band-B ULF energy primaries and the M3 innovation primaries built on
them) is **terminally excluded from the successor's accrual lane
set**. For Window 2 it can never become verdict-bearing.

## What stands, unedited

- `family_b = FILTER_SUPPORT_INSUFFICIENT` — the typed disposition at
  `calibration/mag_family_b_support_disposition_v1.json` (bound,
  byte-frozen), grounded in measured support: 27.37% Newell rows
  missing on carrier `3620d0c2…`; joint-segment median 5 min vs the
  frozen 1,065-sample requirement; zero admissible Family-B days at
  every observatory and both M3 pairs.
- `mag_primary_set = UNTESTABLE_NO_ADMISSIBLE_PRIMARY` — retained
  verbatim, per the ruling's option 1.
- Family A remains a **registered secondary** exactly as worded in
  the bound disposition (codex 1656Z wording repair): it is not
  literally unaffected and cannot rescue the primary gate.
- Every MAG calibration artifact stays BOUND as pinned history: the
  17 registered `calibration/mag_*` paths (10 observatory ledgers, 4
  M3 ledgers, carrier record, receipt, final-bind record) and the
  s4t carrier object `3620d0c2…`. Nothing is unbound, edited, or
  deleted. The captured 212-day × 5-observatory dataset stays banked.

## Rules this disposition binds

1. **No repair-by-reinterpretation**: no interpolation, gap-bridging,
   fallback family, or post-hoc primary re-scoping may be applied to
   make the current MAG design testable inside Window 2.
2. **Power certification**: the anticipated-mask power result package
   (`power_cert_result_contract_v1.md`) records MAG-dependent lanes,
   where they would otherwise appear, as **typed non-certification**
   entries consistent with this exclusion — never as power numbers.
3. **Any future MAG revision is option 2 by definition**: a
   genuinely new preregistered MAG support/design revision with its
   own support and power gates, targeting a future window. The
   exploration track's instrument findings (e.g. energy-domain
   differencing) may inform such a design but remain non-evidence:
   per the ruling, "The exploration track remains non-evidence and
   cannot repair this gate."

## Naming disambiguation (hostile-reader note)

MAG "Family-B" above is the frozen MAG-1 band-B ULF filter family.
It is unrelated to the synthetic power-certification families
`B1B/B2A/B2B/B3A` in `effect_grids_w2_v1.json`, which are shift-null
statistical families and are unaffected by this exclusion.

## Claim ceiling

Lane-set registration only. This disposition is not a scientific
result: the MAG design was neither shown to work nor shown to fail —
its primary endpoint was untestable on the available support.
No power value, no admission, no claim; Λ_geo INCONCLUSIVE.
